# Supabase — same project as the rest of the repo, new `ec_` tables.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Where magic-link emails should redirect back to (must match Supabase Auth
# redirect allow-list). Local dev runs on port 3001.
NEXT_PUBLIC_SITE_URL=http://localhost:3001
